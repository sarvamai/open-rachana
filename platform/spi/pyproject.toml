[project]
name = "mulyankan-spi"
version = "0.1.0"
description = "Provider service-provider interfaces and conformance suites for the Project Rachana Layer 1 workflow core"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mulyankan_spi"]
